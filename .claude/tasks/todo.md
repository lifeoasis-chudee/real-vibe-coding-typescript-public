# TODO 1
Create fastapi module, server

# TODO 2 - Make logger print json string in console correctly
module: air-logger
Currently, the log output of dict, dataclass, and pydantic model by air-logger is single quoted string. It is hard to copy and past as json format. 
Make the console log output of structlog way produce valid json string format.